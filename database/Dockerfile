FROM postgis/postgis:14-3.2

ARG DB_USER
ARG DB_PASSWORD
ARG DB_NAME
ARG DB_PORT

ENV POSTGRES_USER=${DB_USER:-otherthanadmin}
ENV POSTGRES_PASSWORD=${DB_PASSWORD:-secretpassword}
ENV POSTGRES_DB=${DB_NAME:-weatherdatadb}
ENV POSTGRES_PORT=${DB_PORT:-5432}

EXPOSE ${POSTGRES_PORT}
